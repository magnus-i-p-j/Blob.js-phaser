/* globals Phaser: false */
// Loading phaser with requirejs doesn't work for the moment.
// so consider this and the loading in index.html as a workaround
define(function () {
    return Phaser;
});
